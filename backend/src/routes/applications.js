const express = require('express');
const router = express.Router();

// Placeholder for applications routes
router.get('/', (req, res) => {
  res.json({ message: 'Applications routes - to be implemented' });
});

module.exports = router;
