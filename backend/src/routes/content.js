const express = require('express');
const router = express.Router();

// Placeholder for content routes
router.get('/', (req, res) => {
  res.json({ message: 'Content routes - to be implemented' });
});

module.exports = router;
