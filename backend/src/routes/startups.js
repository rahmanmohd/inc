const express = require('express');
const router = express.Router();

// Placeholder for startups routes
router.get('/', (req, res) => {
  res.json({ message: 'Startups routes - to be implemented' });
});

module.exports = router;
